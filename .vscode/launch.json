{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch GLCE",
      "type": "lldb",
      "request": "launch",
      "cwd": "${workspaceFolder}/glce",
      "program": "${workspaceFolder}/glce/bin/gl_choco_engine"
    }
  ]
}
